"""Top-level package for Difix custom modules.

Makes the `src` directory importable, allowing statements like
`from src.pipeline_difix import DifixPipeline` to work when the project
root is on `PYTHONPATH` (which is the default when you run Python from
this directory).
"""
